# Git Ingestion Demo

- Added to verify the live Git → Qdrant / Neo4j ingestion flow.
- Commit message: `chore(demo): add git ingest demo marker`.
- After pushing, run `python scripts/run_activity_ingestion.py --sources git` and query `/git last commit`.

