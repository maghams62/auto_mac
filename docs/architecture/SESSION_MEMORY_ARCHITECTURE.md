# Session Memory System - Architecture Diagram

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER LAYER                              │
│  ┌──────────────────┐              ┌──────────────────┐        │
│  │  Terminal CLI    │              │  WebSocket API   │        │
│  │  (main.py)       │              │  (api_server.py) │        │
│  └────────┬─────────┘              └────────┬─────────┘        │
└───────────┼──────────────────────────────────┼──────────────────┘
            │                                  │
            └──────────────┬───────────────────┘
                           │
┌──────────────────────────┼───────────────────────────────────────┐
│         UI LAYER         ▼                                       │
│  ┌────────────────────────────────────────┐                     │
│  │  ChatUI (src/ui/chat.py)               │                     │
│  │  - Session status indicators           │                     │
│  │  - Prompt with interaction count       │                     │
│  │  - /clear command display              │                     │
│  └────────────────┬───────────────────────┘                     │
└───────────────────┼──────────────────────────────────────────────┘
                    │
┌───────────────────┼──────────────────────────────────────────────┐
│    COMMAND LAYER  ▼                                              │
│  ┌────────────────────────────────────────┐                     │
│  │  SlashCommandHandler                   │                     │
│  │  (src/ui/slash_commands.py)            │                     │
│  │  - Parse /clear command                │                     │
│  │  - Route to SessionManager             │                     │
│  └────────────────┬───────────────────────┘                     │
└───────────────────┼──────────────────────────────────────────────┘
                    │
┌───────────────────┼──────────────────────────────────────────────┐
│   MEMORY LAYER    ▼                                              │
│  ┌────────────────────────────────────────────────────┐         │
│  │         SessionManager                              │         │
│  │         (src/memory/session_manager.py)             │         │
│  │  - Get/create sessions                              │         │
│  │  - Save/load from disk                              │         │
│  │  - Thread-safe access                               │         │
│  │  - Archive old sessions                             │         │
│  └────────────────┬───────────────────────────────────┘         │
│                   │                                              │
│  ┌────────────────▼───────────────────────────────────┐         │
│  │         SessionMemory                               │         │
│  │         (src/memory/session_memory.py)              │         │
│  │  - Conversation history                             │         │
│  │  - Shared context (key-value store)                │         │
│  │  - User preferences                                 │         │
│  │  - Metadata (usage stats)                           │         │
│  └────────────────┬───────────────────────────────────┘         │
└───────────────────┼──────────────────────────────────────────────┘
                    │
┌───────────────────┼──────────────────────────────────────────────┐
│   AGENT LAYER     ▼                                              │
│  ┌────────────────────────────────────────────────────┐         │
│  │  AutomationAgent (src/agent/agent.py)              │         │
│  │  - Receives session context in state               │         │
│  │  - LangGraph execution with memory                 │         │
│  │  - Records interaction after execution             │         │
│  └────────────────┬───────────────────────────────────┘         │
│                   │                                              │
│  ┌────────────────▼───────────────────────────────────┐         │
│  │  AgentRegistry (src/agent/agent_registry.py)       │         │
│  │  - Routes tools to agents                           │         │
│  │  - Tracks tool usage per session                   │         │
│  │  - Auto-stores common results in context           │         │
│  └────────────────┬───────────────────────────────────┘         │
│                   │                                              │
│  ┌────────────────▼───────────────────────────────────┐         │
│  │  MainOrchestrator                                   │         │
│  │  (src/orchestrator/main_orchestrator.py)            │         │
│  │  - Merges session context with execution context   │         │
│  │  - Coordinates planner → executor flow             │         │
│  └────────────────┬───────────────────────────────────┘         │
└───────────────────┼──────────────────────────────────────────────┘
                    │
┌───────────────────┼──────────────────────────────────────────────┐
│  EXECUTION LAYER  ▼                                              │
│  ┌────────────────────────────────────────────────────┐         │
│  │  Planner → Executor → Individual Agents            │         │
│  │  - Access shared context from session               │         │
│  │  - Use previous results (e.g., file paths)         │         │
│  │  - Execute tools with session tracking             │         │
│  └─────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────┘
```

## Data Flow

### Request Flow (User → Agents)

```
User Input
    │
    ▼
┌─────────────────────┐
│  1. SessionManager  │ ─── Load existing session from disk
│     get_session()   │     or create new one
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  2. SessionMemory   │ ─── Retrieve conversation history
│     get_context()   │     and shared context
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  3. Inject into     │ ─── Add session context to
│     LangGraph State │     planning/execution
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  4. Planner         │ ─── Plans with awareness of
│     (with context)  │     previous interactions
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  5. Executor        │ ─── Executes using context
│     (uses context)  │     (e.g., $last_file_path)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  6. Individual      │ ─── Tool execution with
│     Agents/Tools    │     session tracking
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  7. SessionMemory   │ ─── Store interaction results
│     add_interaction │     and update context
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  8. SessionManager  │ ─── Persist session to disk
│     save_session()  │     (auto-save)
└──────────┬──────────┘
           │
           ▼
      User Response
```

### Clear Command Flow

```
User: /clear
    │
    ▼
┌─────────────────────┐
│  1. ChatUI or       │ ─── Detects /clear command
│     SlashCommand    │
│     Handler         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  2. SessionManager  │ ─── Calls clear_session()
│     clear_session() │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  3. SessionMemory   │ ─── Clears all data:
│     clear()         │     - interactions = []
│                     │     - shared_context = {}
│                     │     - metadata reset
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  4. SessionManager  │ ─── Save cleared state
│     save_session()  │     to disk
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  5. UI Update       │ ─── Display:
│     show_status()   │     "🧹 Session Cleared"
└──────────┬──────────┘
           │
           ▼
    Ready for new request
```

## State Transitions

```
┌──────────────┐
│  NEW SESSION │ ◄─────────────────┐
│  (no history)│                   │
└──────┬───────┘                   │
       │                           │
       │ First interaction         │
       ▼                           │
┌──────────────┐                   │
│   ACTIVE     │                   │
│  (has context│                   │
│   + history) │                   │
└──────┬───────┘                   │
       │                           │
       │ /clear command            │
       ▼                           │
┌──────────────┐                   │
│   CLEARED    │───────────────────┘
│ (reset, ready│   Next interaction
│  for fresh   │   treats as NEW
│   start)     │
└──────────────┘
```

## Memory Structure

```
SessionMemory
├── session_id: str
│   └── Unique identifier (e.g., "default", "user123")
│
├── status: SessionStatus
│   ├── ACTIVE    - Normal operation
│   ├── CLEARED   - Just reset
│   └── ARCHIVED  - Old, inactive session
│
├── timestamps
│   ├── created_at: datetime
│   └── last_active_at: datetime
│
├── interactions: List[Interaction]
│   └── [
│       {
│         interaction_id: "int_1",
│         timestamp: "2025-11-10T10:05:00",
│         user_request: "Take a screenshot",
│         agent_response: {...},
│         plan: [...],
│         step_results: {...},
│         metadata: {...}
│       },
│       ...
│     ]
│
├── shared_context: Dict[str, Any]
│   └── {
│       "last_screenshot_path": "/path/to/img.png",
│       "last_presentation_path": "/path/to/file.key",
│       "last_search_results": [...],
│       "user_preference_theme": "dark"
│     }
│
├── user_context: UserContext
│   ├── preferences: {...}
│   ├── frequently_used_tools: [...]
│   └── common_patterns: [...]
│
└── metadata: Dict[str, Any]
    ├── total_requests: int
    ├── total_steps_executed: int
    ├── agents_used: Set[str]
    ├── tools_used: Set[str]
    └── cleared_at: Optional[datetime]
```

## LangGraph Integration

```
┌────────────────────────────────────────────────┐
│          AgentState (LangGraph)                │
├────────────────────────────────────────────────┤
│  # Standard fields                             │
│  - user_request: str                           │
│  - goal: str                                   │
│  - steps: List[Dict]                           │
│  - current_step: int                           │
│  - step_results: Dict[int, Any]                │
│  - messages: List[Any]                         │
│                                                │
│  # NEW: Session fields                         │
│  - session_id: Optional[str] ───────┐         │
│  - session_context: Optional[Dict] ─┼─────┐   │
│    {                                │     │   │
│      "session_id": "user123",       │     │   │
│      "conversation_history": [...], │     │   │
│      "shared_context": {            │     │   │
│        "last_file": "/path"         │     │   │
│      }                               │     │   │
│    }                                 │     │   │
└──────────────────────────────────────┼─────┼───┘
                                       │     │
                              Injected │     │ Used by
                              by Agent │     │ Planner/Executor
                                       │     │
                          ┌────────────▼─────▼────────┐
                          │   SessionMemory.           │
                          │   get_langgraph_context()  │
                          └────────────────────────────┘
```

## Persistence Layer

```
┌─────────────────────────────────────────────────────┐
│                In-Memory Cache                      │
│  SessionManager._sessions: Dict[str, SessionMemory] │
│  {                                                   │
│    "default": SessionMemory(...),                   │
│    "user123": SessionMemory(...),                   │
│    "user456": SessionMemory(...)                    │
│  }                                                   │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ save_session()
                   │ (auto-called after each interaction)
                   ▼
┌─────────────────────────────────────────────────────┐
│              Disk Storage (JSON)                    │
│  data/sessions/                                     │
│  ├── default.json                                   │
│  ├── user123.json                                   │
│  └── user456.json                                   │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ load_session()
                   │ (on startup or new connection)
                   ▼
┌─────────────────────────────────────────────────────┐
│              In-Memory Cache                        │
│  (Session restored from disk)                       │
└─────────────────────────────────────────────────────┘
```

## Context Auto-Storage Rules

```
┌────────────────────────────────────────────────┐
│  Tool Execution in AgentRegistry               │
└────────────────┬───────────────────────────────┘
                 │
                 ▼
         ┌───────────────┐
         │  Tool Name?   │
         └───────┬───────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌──────────┐ ┌──────────┐ ┌────────────┐
│take_     │ │create_   │ │search_     │
│screenshot│ │keynote*  │ │documents   │
└────┬─────┘ └────┬─────┘ └─────┬──────┘
     │            │              │
     ▼            ▼              ▼
Store         Store          Store
last_         last_          last_
screenshot    presentation   search_
_path         _path          results
```

## Thread Safety

```
┌─────────────────────────────────────────┐
│         SessionManager                  │
│  with Lock:  ◄────── Thread-safe        │
│    _sessions: Dict[str, SessionMemory]  │
└─────────────────┬───────────────────────┘
                  │
     ┌────────────┼────────────┐
     │            │            │
     ▼            ▼            ▼
┌─────────┐  ┌─────────┐  ┌─────────┐
│ Thread  │  │ Thread  │  │ Thread  │
│   1     │  │   2     │  │   3     │
│ (User A)│  │ (User B)│  │ (User C)│
└─────────┘  └─────────┘  └─────────┘
```

## Multi-User Architecture

```
┌──────────────────────────────────────────┐
│           WebSocket Server               │
│  ConnectionManager.active_connections:   │
│  {                                       │
│    "session_abc": WebSocket_1,           │
│    "session_xyz": WebSocket_2,           │
│    "session_123": WebSocket_3            │
│  }                                       │
└──────────────────┬───────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────┐
│           SessionManager                 │
│  {                                       │
│    "session_abc": SessionMemory(...),    │
│    "session_xyz": SessionMemory(...),    │
│    "session_123": SessionMemory(...)     │
│  }                                       │
└──────────────────────────────────────────┘

Each user has:
- Unique session_id
- Isolated SessionMemory
- Independent conversation history
- Separate shared context
```

## Summary

This architecture provides:

1. **Clean Separation**: UI → Commands → Memory → Agents → Execution
2. **Bidirectional Flow**: Context flows down, results flow up
3. **Persistence**: Auto-save after each interaction
4. **Thread Safety**: Concurrent access via locks
5. **Multi-User**: Isolated sessions per user
6. **LangGraph Integration**: Context injection into state
7. **Extensibility**: Easy to add new context types
8. **Testability**: Each layer independently testable

The design follows SOLID principles with clear responsibilities at each layer and minimal coupling between components.
