# Agent Router Prompt (placeholder)

The Agent Router currently performs deterministic filtering based on
identified agents and does not rely on an LLM prompt. This file exists as a
placeholder so future iterations can introduce LLM-guided coordination if
needed.
